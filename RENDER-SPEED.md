# Deploy-ის აჩქარება Render-ზე

## პრობლემა

ყოველი განახლებისას Render თავიდან აყენებს ყველა ბიბლიოთეკას. აქამდე ეს იყო
**200 პაკეტი** — და მათგან 86 ისეთი, რაც სერვერს **საერთოდ არ სჭირდება**:
Capacitor-ის ხელსაწყოები მხოლოდ შენს კომპიუტერზე გამოიყენება APK-ის ასაწყობად.

## რა უკვე გაკეთდა (კოდში)

`package.json`-ში ბიბლიოთეკები ორ ჯგუფად გაიყო:

- `dependencies` — რაც **სერვერს** სჭირდება გასაშვებად
- `devDependencies` — რაც მხოლოდ **შენს კომპიუტერს** სჭირდება (APK, ტესტები)

გაზომილი შედეგი ჩემს მანქანაზე:

| ბრძანება | დრო | პაკეტი |
|---|---|---|
| `npm install` (ძველი) | 16.1 წმ | 200 |
| `npm ci --omit=dev` (ახალი) | 4.3 წმ | 114 |

Render-ის უფასო მანქანა ჩემზე ბევრად ნელია, ამიტომ იქ სხვაობა უფრო დიდი იქნება.

## რა უნდა გააკეთო შენ — ერთი ველი

1. გახსენი **https://dashboard.render.com/** → შენი სერვისი `domino`
2. მარცხნივ **Settings**
3. იპოვე **Build Command** — ახლა იქ სავარაუდოდ წერია `npm install`
4. შეცვალე ამით:
   ```
   npm ci --omit=dev
   ```
5. **Save Changes**

> `npm ci` ზუსტად იმ ვერსიებს აყენებს, რაც `package-lock.json`-შია ჩაწერილი —
> უფრო სწრაფიცაა და უფრო საიმედოც (ორივეგან ერთი და იგივე დაყენდება).
> `--omit=dev` კი ეუბნება, რომ ის 86 ზედმეტი პაკეტი გამოტოვოს.

## რაც მაინც დარჩება

Render-ის უფასო გეგმაზე deploy-ს ისედაც სჭირდება დრო: კოდის ჩამოტვირთვა,
სერვისის თავიდან აშენება და გაშვება. ბიბლიოთეკების დაყენება ამის მხოლოდ
ერთი ნაწილია — ჩვენ სწორედ ის შევამცირეთ. სრული deploy მაინც დარჩება
1-2 წუთის ფარგლებში.

**გახსოვდეს:** ეს არის სამუშაო სერვერის განახლება. ტესტები (`npm test`) შენს
კომპიუტერზე რამდენიმე წამში გადის — ანუ შეცდომა Render-ის ლოდინამდე იჭერა.
