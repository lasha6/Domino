# Deploy-ის აჩქარება Render-ზე

## რა უნდა გააკეთო — ერთი ცვლადი

**იმავე გვერდზე, სადაც Google-ის გასაღები ჩაწერე:**

1. **https://dashboard.render.com/** → შენი სერვისი `domino`
2. მარცხნივ **Environment** → **Add Environment Variable**
   - Key: `NODE_ENV`
   - Value: `production`
3. **Save Changes**

სულ ეს არის. Build Command-ს ხელი არ ახლო — არ არის საჭირო.

## რატომ მუშაობს ეს

ყოველი განახლებისას Render თავიდან აყენებს ბიბლიოთეკებს. მათგან 86 ისეთია,
რაც სერვერს **საერთოდ არ სჭირდება** — Capacitor-ის ხელსაწყოები მხოლოდ შენს
კომპიუტერზე მუშაობს APK-ის ასაწყობად.

`package.json`-ში ისინი უკვე გამოვყავი ცალკე ჯგუფად (`devDependencies`).
`NODE_ENV=production` კი npm-ს ეუბნება, რომ ეს ჯგუფი გამოტოვოს.

გაზომილი, სუფთა საქაღალდეში, ორივე ვარიანტი:

| | პაკეტი |
|---|---|
| როგორც ახლაა | 203 |
| `NODE_ENV=production`-ით | 114 |

Render-ის უფასო მანქანა ჩემს კომპიუტერზე ბევრად ნელია, ამიტომ იქ სხვაობა
უფრო შესამჩნევი იქნება.

## თუ Build Command მაინც გინდა (არასავალდებულო)

გაზომვით ეს **დამატებით თითქმის ვერაფერს იძლევა** — ზემოთ ერთი ცვლადი უკვე
საკმარისია. თუ მაინც გინდა:

Render → შენი სერვისი → მარცხნივ **Settings** → ჩამოქაჩე **Build & Deploy**
განყოფილებამდე → **Build Command** → `npm ci --omit=dev`

(`npm ci` ზუსტად იმ ვერსიებს აყენებს, რაც `package-lock.json`-შია — ოდნავ
უფრო საიმედოა, მაგრამ სისწრაფეში სხვაობა მცირეა.)

## რაც მაინც დარჩება

Render-ის უფასო გეგმაზე deploy-ს ისედაც სჭირდება დრო: კოდის ჩამოტვირთვა,
სერვისის აშენება, გაშვება და შემოწმება. ბიბლიოთეკები ამის მხოლოდ ერთი
ნაწილია — სწორედ ის შევამცირეთ. სრული deploy 1-2 წუთის ფარგლებში დარჩება.

**გახსოვდეს:** შეცდომის დასაჭერად Render-ს ლოდინი არ სჭირდება — `npm test`
შენს კომპიუტერზე ~1 წუთში ამოწმებს ყველაფერს.
